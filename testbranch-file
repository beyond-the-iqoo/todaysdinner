branchに関するテスト

git branch <branch name>
ブランチを作成

git checkout <branch name>
現在のbranchを変更

これでadd,commit,pushすれば別ブランチが表示されるはず？

----

git push origin <branch name>
でブランチを指定しないとだめ？checkoutがよくわからん?
 -> git push origin <branch name>は最初だけでイイっぽい
